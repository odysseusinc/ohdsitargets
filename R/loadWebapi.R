# TODO build trivial utility functions for loadWebApi

#set the baseurl to webapi
setUrl <- function(webApiUrl) {
  webApiUrl
}

#set the cohortIds to extract from webapi
setCohortIds <- function(cohortIds) {
  cohortIds
}